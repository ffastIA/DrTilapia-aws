## 1. Preparação

- [x] 1.1 Definir `GENERIC_ERROR_MESSAGE` (constante compartilhada) em `backend/app/main.py` ou um módulo de erros comum.
- [x] 1.2 Listar as 28 ocorrências atuais de `str(e)` em `backend/app/main.py` (`grep -n "str(e)" backend/app/main.py`) e classificar cada uma como "segura" (mensagem de domínio deliberada) ou "insegura" (exceção genérica de biblioteca). Resultado: 9 seguras (`except ValueError`/`except PermissionError` com mensagem de domínio: profile 404/422, upload_video/upload_fish_image 400, delete_video/delete_fish_image/delete_fish_analysis 404/403), 18 inseguras (`except Exception` genérico), 1 fora de escopo (linha ~682 — grava `str(e)` num campo do banco `processing_error`, não é resposta HTTP; registrado como achado futuro separado, não corrigido nesta change).

## 2. Aplicar correção nas ocorrências inseguras

- [x] 2.1 Substituir cada `detail=f"...: {str(e)}"` / `detail=str(e)` classificado como inseguro por `detail=GENERIC_ERROR_MESSAGE`, adicionando `logger.exception(...)` imediatamente antes. As 18 ocorrências já tinham `logger.exception(...)` na linha anterior (mantido); só a variável `e` deixou de ser referenciada no `detail=` (o `except Exception as e:` virou `except Exception:` onde `e` não era mais usado em nenhum outro lugar do bloco).
- [x] 2.2 Revisar o status HTTP de cada ocorrência corrigida — ajustar para 400/422 quando o erro na verdade reflete entrada inválida do cliente, mantendo 500 apenas para falhas genuinamente do servidor. Todas as 18 ocorrências já eram `except Exception` genérico (não domínio) mantido em `500` — os casos de entrada inválida do cliente já tinham blocos dedicados `except ValueError`/`except PermissionError` com 400/403/404/422 próprios (as 9 ocorrências "seguras"), então nenhum ajuste de status foi necessário.
- [x] 2.3 Ocorrências classificadas como seguras (mensagem de domínio) permanecem inalteradas — documentar quais foram mantidas e por quê. Mantidas em `backend/app/main.py`: `get_profile`/`upsert_profile` (404/422 — perfil não encontrado/inválido), `upload_video`/`upload_fish_image` (400 — formato de arquivo não suportado), `delete_video`/`delete_fish_image`/`delete_fish_analysis` (404/403 — recurso não encontrado/sem permissão). Todas são `ValueError`/`PermissionError` levantadas deliberadamente pela própria camada de serviço com mensagem já pensada para o usuário final.

## 3. Verificação

- [x] 3.1 `grep -n "str(e)" backend/app/main.py` — confirmar que só restam as ocorrências explicitamente classificadas como seguras. Confirmado: restam exatamente as 9 ocorrências seguras (`get_profile`/`upsert_profile`/`upload_video`/`upload_fish_image`/`delete_video`/`delete_fish_image`/`delete_fish_analysis`) + a 1 fora de escopo (linha ~688, gravação em `processing_error` no banco, não é resposta HTTP). Nenhuma ocorrência insegura restante.
- [x] 3.2 Simular uma falha de biblioteca (ex.: desligar a rede para o Supabase temporariamente) em um endpoint corrigido → confirmar que a resposta ao cliente é genérica e que o log do servidor contém o stack trace completo. Testado via `TestClient` dentro do container contra `GET /admin/vector-base/files` (com admin mockado): o container não conseguiu resolver o host do Supabase (`httpx.ConnectError: [Errno -2] Name or service not known`) — uma falha de biblioteca real, não simulada. Resposta ao cliente: `500` / `{"detail": "Erro interno. Tente novamente mais tarde."}` (sem nenhum detalhe de infraestrutura). Log do servidor: `logger.exception` registrou o traceback completo, incluindo a cadeia httpx → httpcore → `ConnectError`.
- [x] 3.3 Rodar a suíte `pytest` existente (`backend/tests/`) para confirmar que nenhum teste dependia do texto de exceção específico em uma resposta de erro. Resultado: 121 passed, 12 failed, 11 skipped — idêntico ao baseline documentado em `backend-dependency-hygiene` e `upload-validation-and-limits` (as mesmas 12 falhas pré-existentes: `/health` inexistente, métodos `recover_file_content`/`diagnose_file_recovery` ausentes em `vector_admin_service` — H3 —, teste de integração que exige servidor real). Nenhuma falha nova, nenhum teste dependia do texto de `str(e)` vazado.
